# Variable used to "sign" each cookie to avoid tampering. This will be encrypted and should never be shared.
SESSION_SECRET=""

# Variables used for connecting to your local Postgres server
PGHOST=localhost
PGPORT=5432
PGDATABASE=users_db
PGUSER=""
PGPASSWORD=""

# When deploying, set this to your hosted database URL instead of the vars above
PG_CONNECTION_STRING=""
